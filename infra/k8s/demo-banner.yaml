apiVersion: v1
kind: ConfigMap
metadata:
  name: clara-demo-banner
  namespace: clara-voiceops
  labels:
    app.kubernetes.io/name: clara-voiceops
    app.kubernetes.io/component: gitops-demo
data:
  BANNER_TEXT: "Clara VoiceOps v0.0.5 — only Aadi + Bunny cloned voices (no generic premade fallback)"
  RELEASE_NOTES: |
    0.0.5
    -----
    - ElevenLabs: doctor uses cloned sample (Dr. Aadi), pharmacist uses cloned sample (Bunny).
    - Removed premade generic ElevenLabs voice and extra demo doctor from seed data.
    - Worker/API fallbacks map doctor vs pharmacist to those clone IDs only.
  GITOPS_NOTE: "Edit infra/k8s/demo-banner.yaml, commit to main, ArgoCD will auto-sync within 3 minutes."
